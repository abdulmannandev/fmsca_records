import React from 'react';
// import PivotTableUI from 'react-pivot';
// import 'react-pivot/dist/styles.css';

const PivotView = ({ data, onSettingsChange, settings }) => (
//   <PivotTableUI
//     data={data}
//     onChange={(newSettings) => onSettingsChange({ ...settings, pivot: newSettings })}
//     {...settings.pivot}
//   />
null
);

export default PivotView;
